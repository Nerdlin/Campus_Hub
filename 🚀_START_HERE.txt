╔══════════════════════════════════════════════════════════════════════════════╗
║                    CAMPUS HUB - ФИНАЛЬНЫЙ ДЕПЛОЙ                             ║
║                          Digital Ocean Droplet                               ║
╚══════════════════════════════════════════════════════════════════════════════╝

📍 Сервер: 164.92.104.243
👤 Пользователь: root (затем используется campus)
🎯 Цель: Запустить приложение доступное по http://164.92.104.243

╔══════════════════════════════════════════════════════════════════════════════╗
║  ШАГ 1: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ                                               ║
╚══════════════════════════════════════════════════════════════════════════════╝

В вашем открытом SSH терминале вы уже подключены как root@campus-hub

╔══════════════════════════════════════════════════════════════════════════════╗
║  ШАГ 2: ВЫПОЛНИТЬ АВТОМАТИЧЕСКИЙ ДЕПЛОЙ                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

Скопируйте и вставьте ВСЮ КОМАНДУ НИЖЕ (одной строкой):

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

sudo systemctl stop campus-hub.service campus-hub-api.service 2>/dev/null || true && sudo rm -rf /var/www/Campus_Hub/.next && cd /var/www/Campus_Hub && sudo -u campus git pull && sudo chown -R campus:campus /var/www/Campus_Hub && sudo chmod -R 750 /var/www/Campus_Hub && sudo chmod -R 770 /var/www/Campus_Hub/uploads && sudo -u campus npm run build && sudo cp /var/www/Campus_Hub/deploy/systemd/campus-hub*.service /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable campus-hub-api.service campus-hub.service && sudo systemctl start campus-hub-api.service && sleep 3 && sudo systemctl start campus-hub.service && sleep 3 && SERVER_IP=$(curl -s ifconfig.me) && sudo cp /var/www/Campus_Hub/deploy/nginx/campus-hub.conf /etc/nginx/sites-available/campus-hub.conf && sudo sed -i "s/YOUR_DOMAIN/$SERVER_IP/g" /etc/nginx/sites-available/campus-hub.conf && sudo rm -f /etc/nginx/sites-enabled/default && sudo ln -sf /etc/nginx/sites-available/campus-hub.conf /etc/nginx/sites-enabled/ && sudo nginx -t && sudo systemctl reload nginx && echo "" && echo "==========================================" && echo "✅ ДЕПЛОЙ ЗАВЕРШЁН УСПЕШНО!" && echo "==========================================" && echo "🌐 Ваше приложение доступно по адресу:" && echo "   http://$SERVER_IP" && echo "" && echo "📊 Статус сервисов:" && sudo systemctl status campus-hub-api.service --no-pager -l | grep -E "(Active|Main PID)" && sudo systemctl status campus-hub.service --no-pager -l | grep -E "(Active|Main PID)" && echo "" && echo "🔌 Открытые порты:" && sudo ss -tlnp | grep -E ':(3000|4000|80)' && echo "" && echo "🧪 Проверка доступности:" && curl -I http://localhost:3000 2>/dev/null | head -1 && curl -I http://localhost:4000 2>/dev/null | head -1 && echo "==========================================="

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏱️  Время выполнения: ~2-3 минуты

╔══════════════════════════════════════════════════════════════════════════════╗
║  ЧТО ДЕЛАЕТ ЭТОТ СКРИПТ:                                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

✅ Останавливает старые сервисы
✅ Удаляет кеш сборки Next.js
✅ Загружает последний код с GitHub
✅ Исправляет права доступа
✅ Собирает Next.js приложение
✅ Обновляет systemd сервисы
✅ Запускает API и Next.js
✅ Настраивает Nginx
✅ Проверяет работоспособность

╔══════════════════════════════════════════════════════════════════════════════╗
║  ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:                                                        ║
╚══════════════════════════════════════════════════════════════════════════════╝

После выполнения вы увидите:

==========================================
✅ ДЕПЛОЙ ЗАВЕРШЁН УСПЕШНО!
==========================================
🌐 Ваше приложение доступно по адресу:
   http://164.92.104.243

📊 Статус сервисов:
   Active: active (running)
   Active: active (running)

🔌 Открытые порты:
   LISTEN    0     511    0.0.0.0:3000
   LISTEN    0     511    0.0.0.0:4000
   LISTEN    0     511    0.0.0.0:80

🧪 Проверка доступности:
   HTTP/1.1 200 OK
   HTTP/1.1 200 OK
==========================================

╔══════════════════════════════════════════════════════════════════════════════╗
║  ШАГ 3: ОТКРЫТЬ В БРАУЗЕРЕ                                                   ║
╚══════════════════════════════════════════════════════════════════════════════╝

Откройте в браузере:
👉 http://164.92.104.243

Вы должны увидеть главную страницу Campus Hub!

╔══════════════════════════════════════════════════════════════════════════════╗
║  ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК                                                    ║
╚══════════════════════════════════════════════════════════════════════════════╝

1️⃣  Если сборка упала на "Module not found":
    
    sudo rm -rf /var/www/Campus_Hub/.next /var/www/Campus_Hub/node_modules
    cd /var/www/Campus_Hub
    sudo -u campus npm install
    sudo -u campus npm run build

2️⃣  Проверка логов:
    
    sudo journalctl -u campus-hub-api.service -n 50 --no-pager
    sudo journalctl -u campus-hub.service -n 50 --no-pager

3️⃣  Перезапуск всего:
    
    sudo systemctl restart campus-hub-api.service
    sudo systemctl restart campus-hub.service
    sudo systemctl reload nginx

4️⃣  Проверка портов:
    
    sudo ss -tlnp | grep -E ':(3000|4000|80)'

5️⃣  Ручная проверка доступности:
    
    curl http://localhost:3000
    curl http://localhost:4000
    curl http://164.92.104.243

╔══════════════════════════════════════════════════════════════════════════════╗
║  ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ (ОПЦИОНАЛЬНО)                                      ║
╚══════════════════════════════════════════════════════════════════════════════╝

🔒 Установка HTTPS (если есть домен):

    sudo apt install -y certbot python3-certbot-nginx
    sudo certbot --nginx -d your-domain.com

🛡️  Настройка безопасности:

    sudo apt install -y fail2ban unattended-upgrades
    sudo dpkg-reconfigure --priority=low unattended-upgrades

💾 Настройка автоматических бэкапов:

    sudo mkdir -p /var/backups/campus-hub
    cat << 'EOF' | sudo tee /usr/local/bin/backup-campus.sh
    #!/bin/bash
    DATE=$(date +%Y-%m-%d_%H-%M-%S)
    cd /var/www/Campus_Hub
    tar czf /var/backups/campus-hub/campus-$DATE.tar.gz db.json uploads/
    find /var/backups/campus-hub -name "campus-*.tar.gz" -mtime +7 -delete
    EOF
    sudo chmod +x /usr/local/bin/backup-campus.sh
    echo "0 2 * * * root /usr/local/bin/backup-campus.sh" | sudo tee /etc/cron.d/campus-backup

╔══════════════════════════════════════════════════════════════════════════════╗
║  ОБНОВЛЕНИЕ ПРИЛОЖЕНИЯ В БУДУЩЕМ                                             ║
╚══════════════════════════════════════════════════════════════════════════════╝

Когда нужно обновить код после изменений:

    cd /var/www/Campus_Hub
    sudo -u campus git pull
    sudo -u campus npm ci
    sudo -u campus npm run build
    sudo systemctl restart campus-hub-api.service
    sudo systemctl restart campus-hub.service

╔══════════════════════════════════════════════════════════════════════════════╗
║  ПОЛЕЗНЫЕ КОМАНДЫ                                                            ║
╚══════════════════════════════════════════════════════════════════════════════╝

Проверка статуса:
    sudo systemctl status campus-hub.service
    sudo systemctl status campus-hub-api.service

Просмотр логов в реальном времени:
    sudo journalctl -u campus-hub.service -f
    sudo journalctl -u campus-hub-api.service -f

Перезапуск nginx:
    sudo nginx -t
    sudo systemctl reload nginx

Проверка конфигурации nginx:
    sudo cat /etc/nginx/sites-enabled/campus-hub.conf

Использование ресурсов:
    htop
    df -h

╔══════════════════════════════════════════════════════════════════════════════╗
║  АРХИТЕКТУРА ПРИЛОЖЕНИЯ                                                      ║
╚══════════════════════════════════════════════════════════════════════════════╝

Internet → Nginx (port 80/443)
              ↓
         ┌────┴────┐
         │         │
    Next.js      API
   (port 3000) (port 4000)
         │         │
         └────┬────┘
              ↓
         db.json + uploads/

╔══════════════════════════════════════════════════════════════════════════════╗
║  ГОТОВО!                                                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

После выполнения команды из ШАГ 2, ваше приложение будет доступно в интернете!

📱 Ссылка для доступа: http://164.92.104.243

Приятного использования! 🎉
